os: linux
dist: focal
language: bash
services:
  - docker
before_install:
  - docker pull debian:bullseye
script:
  - docker run -v $TRAVIS_BUILD_DIR:/work --rm -it debian:bullseye /bin/bash -c "cd /work/esa/opengeode ; ./run_tests.sh"
